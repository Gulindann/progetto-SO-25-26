#include "headers/scheduler.h"

void scheduler(){
    while(1)
    {
        if(process_count==0) // HALT BIOS
        {

        }
        else if(Process Count > 0 and Soft-block Count > 0 ) //
        {

        }
        else if()
        {

        }
        else // caso default, dove rimuovi il pcb dalla testa dei RQ, inizi il timer di 5ms, fai un load processor state yada yada yada
        {

        }
    }
}